#include <iostream>
#include <cstdio> // For scanf and printf

using namespace std;

int main() {
    int N, C;
    
    // Use scanf to read two integers in a single line
    scanf("%d %d", &N, &C);
    
    cout << N+C;
    return 0;
}
